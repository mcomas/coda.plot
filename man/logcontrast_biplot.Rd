% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clr_ciplot.R
\name{logcontrast_biplot}
\alias{logcontrast_biplot}
\title{Log-contrast biplot}
\usage{
logcontrast_biplot(
  X,
  lc,
  group = NULL,
  shape_group = NULL,
  part_labels = NULL,
  standardize_lc = FALSE,
  rescale_rays = TRUE,
  ray_scale = 1,
  return_data = FALSE,
  repel = TRUE,
  repel_force = 1,
  repel_max_overlaps = Inf
)
}
\arguments{
\item{X}{A matrix or data frame containing compositional data (strictly positive).}

\item{lc}{Numeric matrix with \code{ncol(X)} rows and at least two columns.
Each column must be a log-contrast vector with zero sum. If more than two
columns are supplied, only the first two are used. Column names, when
available, are used as axis labels.}

\item{group}{Optional factor/character used to color the observations.}

\item{shape_group}{Optional factor/character used to assign shapes to observations.}

\item{part_labels}{Optional character vector with part labels. If \code{NULL},
\code{colnames(X)} are used when available.}

\item{standardize_lc}{Logical. If \code{TRUE}, normalize each log-contrast
vector to Euclidean norm 1 before computing coordinates.}

\item{rescale_rays}{Logical. If \code{TRUE}, rescale the part rays to have a
range comparable to the observation coordinates.}

\item{ray_scale}{Numeric multiplier applied to ray lengths after optional
rescaling.}

\item{return_data}{Logical. If TRUE, returns a list with data frames for
observations, parts, and the ggplot object.}

\item{repel}{Logical. If TRUE (default), use ggrepel for part labels when available.}

\item{repel_force}{Numeric. Repulsion force passed to \code{ggrepel::geom_text_repel()}.}

\item{repel_max_overlaps}{Numeric. Maximum overlaps allowed (ggrepel).}
}
\value{
A \code{ggplot2} object. If \code{return_data = TRUE}, a list with elements
  \code{obs}, \code{parts}, and \code{plot}.
}
\description{
Represents compositional observations in the two-dimensional coordinate system
defined by two log-contrasts, and overlays the projected unit directions for
each part as rays from the center of the observations.
}
\examples{
X <- milk_cows[, 5:10]
lc <- cbind(
  protein = c(1, -1, 0, 0, 0, 0),
  fat = c(0, 0, 1, -1, 0, 0)
)
logcontrast_biplot(X, lc, group = milk_cows$group)

}
